# Weekly version updates. Security updates are enabled in the repository settings.
# CI merges green non-major updates, and a bundled package update is released
# automatically, so new versions wait a week before Dependabot proposes them.
version: 2
updates:
  - package-ecosystem: composer
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
    commit-message:
      prefix: chore(deps)
    groups:
      # Dev tools in one PR; runtime dependencies stay separate.
      dev-dependencies:
        dependency-type: development

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
    commit-message:
      prefix: ci
    groups:
      github-actions:
        patterns:
          - '*'
        # The deploy action only runs on release tags, so PR CI never tests it.
        # A separate PR keeps its updates visible for a manual review.
        exclude-patterns:
          - 10up/action-wordpress-plugin-deploy
